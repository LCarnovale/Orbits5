import math, turtle, sys, random
import vector as VECTOR
import particle as PARTICLE

vector = VECTOR.vector()
randomVector = VECTOR.randomVector

class Item:
	def __init__(self, position = None)

class MainLoop:
	def __init__(self, items = []):
		self.items = items
